targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: standard|lib/graphql/schema.graphql
          output_dir: "models"
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: standard|lib/graphql/schema.graphql
          output_dir: "models"
